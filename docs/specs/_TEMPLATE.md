# コンテンツ名

状態: 実装済み / 一部実装 / 未実装・仕様確定 / 未実装・構想段階

## 対象範囲

このファイルが所有する仕様と、他ファイルが所有する境界を書く。

## 現在の実装仕様

- コード上で現在動作する内容だけを書く。
- 解放条件、基本フロー、正確なルール、例外を記録する。

## 主な状態と実装

- セーブされる状態フィールド。
- 主な実装ファイル・関数。
- 世代交代、退出、失敗時のリセット境界。

## 他コンテンツとの依存関係

- 依存先の仕様書へリンクし、同じルールを複製しない。

## 確定している将来仕様

- ユーザーが実装予定として確定した内容。なければ `なし。` と書く。

## 構想段階の案

- 候補・方向性。現在の実装として扱わない。

## 未決定事項

- 実装前にユーザー判断が必要な点。

## 既知の不一致・未検証

- コードと仕様書の不一致、ブラウザ未検証、旧セーブ未検証など。

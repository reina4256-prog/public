# AI育成ゲーム - 転生編 システム仕様書 (Master Specification)

## 1. プロジェクト概要
クォータービューのマップ上でAIキャラクターを育成・観察するシミュレーションゲーム。
HTML5 Canvas + Vanilla JavaScriptによるMVC風アーキテクチャ。
性格診断、全11種族の進化ツリー、「師匠・弟子入りシステム」に加え、新たに「カードバトル(TCG)システム」や「クラウドセーブ連携」を搭載し、大幅に拡張されている。

## 2. ファイル構成と役割 (全15ファイル構成想定)
AIがコードの役割を推論するためのモジュールマップ。

### 2.1 コア・システム系
* `index.html` / `style.css`: UIの土台、各種メニュー、Canvasコンテナ、エフェクト定義。
* `main.js`: エントリポイント。ゲームループの制御、初期化、モード切替、開発・デバッグ用機能（座標微調整など）。
* `system.js`: グローバル変数管理、マップ自動生成、ローカルセーブ/ロード、多言語対応、進化・性格診断のベースロジック。
* `cloud_manager.js` **[NEW]**: クラウドサーバーとの通信、セーブデータのオンライン同期・バックアップ管理。
* `data.js`: マスターデータ群（画像ソース、マップチップ、施設、進化ツリー、性格別セリフ定義）。

### 2.2 ロジック・コントローラー系
* `ai_core.js`: 【中核ロジック】AIのステータス管理、経路探索、タスクキュー実行、師匠クエスト進行管理、作業時の素材自動補充、過労防止セーフティ。
* `game_manager.js`: プレイヤーのアクション（農業、建築、売買）、経済システム（借金・破産処理）の管理。
* `tcg_core.js` **[NEW]**: トレーディングカードゲームのコアルール、デッキ管理、カードの効果処理、バトル進行のロジック。
* `ui_controller.js`: DOM操作、パネルUI更新、チャット入力による行動指示判定、演出（進化ルーレット等）の制御。
* `view_renderer.js`: Canvasへのクォータービュー描画、カメラ追従、アニメーション更新。

### 2.3 エンドコンテンツ・拡張系 (別ファイル想定)
* `feature_pokedex.js`: 図鑑機能、発見キャラ閲覧、放牧マップへの配置UI制御。
* `feature_grazing.js`: 【エンドコンテンツ】放牧システムの独立ディレクターロジック、自律作業アニメーション、放置ボーナス計算。
* `feature_defense.js`: 【島防衛システム】王城を拠点としたタワーディフェンス機能。
  - **襲撃イベント**: 放置時間経過でランダムに魔物が襲来。施設へのダメージは `assets` (大元データ)とリアルタイム同期される。
  - **戦闘アニメーション**: スパロボ風のカットシーン演出（近接、射撃、地震などの専用エフェクト）を実装。
  - **復興システム**: 破壊された施設はリッチUIによるダイアログを通じて、資金(Gold)を消費して復旧、または放棄を選択可能。
* `feature_arena.js` **[NEW]**: 【エンドコンテンツ】闘技場システム。通常エンドレス、ボスラッシュ、他プレイヤーデータと戦うフレンド(幻影)バトル。非同期ランキング通信。
* `feature_dungeon.js` **[NEW]**: 【エンドコンテンツ】ターン制ローグライク探索型ダンジョン（スカル、クリスタル）。視界判定、アイテム持ち込み/持ち帰り、クラウド連携による他プレイヤーの救助要請システムを実装。

## 3. コアデータ構造 (グローバルステート)
* `assets`: マップ上の全オブジェクト（地形、施設、師匠のキャンプ等）の連想配列。
* `aiPet`: AIキャラの全ステータス（欲求、能力、熟練度）。
  * `apprentice`: 師匠・弟子入り状態（現在の師匠、ランク、課題進捗、皆伝履歴(retired)等）。
  * `learnedWords`: チャットから学習した単語リスト。
* `grazingData`: 放牧マップ（最大5つ）の独立した箱庭状態。
* **[NEW想定]** `tcgData`: 所持カード、現在編成中のデッキ情報、バトル戦績など。

## 4. 主要システムの特徴（AIコンテキスト用）
* **行動決定モデル**: チャット入力（指示）と、性格ベースの自律行動（Idle時の確率発動）のハイブリッド。学習済みの言葉のみタスク化可能。
* **師匠・弟子入りシステム**: フィールドエンカウント → 拠点出現 → ランク1〜9の課題進行。詰み防止のための「素材の裏方自動補充」が機能。
* **クラフト＆ペナルティ**: 各職業のアクション時、能力値（賢さ、活力など）が低いと失敗アイテム（鉄くず、黒焦げ等）ができ、能力値が高いと「レアアイテム（質のいい野菜、精巧な建築模型等）」が完成する。
* **AIセーフティ**: 別タブ放置等の超高速時間経過時、満腹度や体力が0になった時点でタスクを強制中断する「過労ストッパー」と、移動失敗時の「スタック救出機能」を搭載。
* **進化＆性格システム**: ステータスの偏りから11種類の性格（のんびり屋、韋駄天など）をリアルタイム判定。全119進化形態それぞれに、性格別の専用セリフ（約1,300パターン）が実装されている。
* **各種エディタ**: `main.js`等のAI Adjust Modeにより、キャラや家具の座標（`sx`, `sy`, `sw`, `sh`）をリアルタイム調整・出力可能。

## 5. 統合デバッグコンソール (Debug Overlay)
ゲーム中に `Shift + D` で起動可能な、アコーディオン式の開発・テスト用統合パネル。以下の機能を持つ。
1. **システム＆リセット**: マップ、AI、放牧データの選択的初期化。
2. **弟子入りシステム**: 任意の師匠（全6職）およびランク（0〜10）への強制変更。ランク0指定で特定の職業履歴のみをピンポイントで未入門状態にリセット可能。
3. **キャラクター操作＆環境**: 無敵モード切替、現在姿の変更、進化条件の確認。Gen/Age、時間、季節の操作。
4. **ステータス＆アイテム**: 熟練度、基本生存パラメータ（体力・満腹度・寿命）、能力値、所持金の書き換え。マップオブジェクトと所持アイテムの直接操作。
5. **防衛＆演出テスト**: 襲撃イベントのON/OFF、防衛戦の強制発生、復興UIテスト、任意種族同士の演出(スパロボ風)確認。アリーナおよびダンジョンの「任意WAVE/階層からの強制スタート」。
6. **TCGテストツール**: 全カードデータのリセット、カジノの強制開放、任意カードの取得。